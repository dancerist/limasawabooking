# limasawabooking.com — Astro Build

> Read this file before every session. Update at end of each session.

## Stack
- **Frontend:** Astro 6, Tailwind CSS v4 — `src/pages/`
- **Backend / Admin:** WordPress at `https://admin.limasawabooking.com` (headless, REST + GraphQL)
- **WP API base:** `https://admin.limasawabooking.com/wp-json/limasawa/v1`
- **GraphQL:** `https://admin.limasawabooking.com/graphql` (WPGraphQL) — to be removed (going REST-native, per siargao's final architecture)
- **Auth:** custom JWT (HS256) via `limasawa/v1/auth/{register,login}` + `limasawa/v1/me`, token stored in `localStorage` as `sb_token`. Two roles: `guest` + `host`.
- **Rendering:** Static — no SSR
- **Permalinks:** pretty `/%postname%/` (enabled 2026-05-30; was plain, which broke `/wp-json/` routing)

## Backend code convention (limasawa-specific — differs from siargao)
- Custom PHP lives in **Novamira sandbox files** at `wp-content/novamira-sandbox/*.php` (auto-loaded), NOT WPCodeBox. limasawa's WAF does NOT block PHP file writes (siargao's did — that's why siargao used WPCodeBox).
- **Source of truth is the git repo `wp/sandbox/*.php`.** Deploy by copying file contents to the server via `novamira/write-file`. Roll back instantly with `novamira/disable-file`.
- **JWT secret** is `SB_JWT_SECRET` in `wp-config.php` (NOT in any snippet, sandbox file, or git). `SB_JWT_ACCESS_TTL` also there (7 days).
- Guard every function with `if (!function_exists())` and keep one owner per function to avoid "Cannot redeclare" fatals.

### Backend files (`wp/sandbox/` → server `wp-content/novamira-sandbox/`)
| File | Purpose |
|---|---|
| `limasawa-auth.php` | Roles (guest/host) + JWT helpers + auth REST API (`/auth/register`, `/auth/login`, `GET/POST /me`) + CORS. Enforces `sb_user_suspended` (login 403, JWT resolver → null). Live & verified 2026-05-30. |
| `limasawa-listings.php` | Listings REST API: `GET /listings` (cards + rich filters), `GET /listings/{slug}` (plural — `{listing:{…,detail}}`), **`GET /listing/{slug}` (SINGULAR — rich flat shape the `stay/[slug].astro` client-renderer fetches: `galleryFull`/`amenityNames`/`faqAmenities` short-keys/`exclusions`/`customInfo`/`claimable`/`hostId`/flat link keys)**, `GET /map-pins`, `GET /taxonomies`. Adds `ratingAvg`/`ratingCount` to cards. Reads native ACF `accommodation` CPT. All rate reads go through `sb_rate_summary()` → card/single shapes expose `priceMode`/`pricingMode`, `priceFrom`, `roomTypes[]`, `perPax{}`, `additionalBedRate`, `pricingExtras[]`. Live & verified 2026-06-01. |
| `limasawa-submit.php` | Listing-submission + subscription: `POST /submit-listing` (JWT host → pending accommodation, payload → ACF + taxonomies + media + tier/payment meta), `POST /upload-media`, `GET /coupon/validate` (codes in `sb_coupons` option), `POST /renew-subscription` (server-priced, coupon re-validated, writes `sb_payment` pending_review; tier upgrades only on admin approval). Tiers free/pro/featured. Manual e-wallet (no gateway). Live & verified 2026-06-01. |
| `limasawa-rentals.php` | **Rentals vertical** (vehicle rentals, admin-listed, public read-only). Attaches the shared `location` taxonomy to the `rental` CPT. REST: `GET /rentals` (cards + `?type`/`?location` filters), `GET /rental/{slug}` (flat single), `GET /rental-types` (facets), `GET /rental-pins` (map). **Host self-listing (JWT):** `POST /submit-rental` (→ pending, owner, `listing_tier`=free), `GET /my-rentals`, `GET /rental-edit/{id}` + `POST /update-rental/{id}` (owner/admin), `POST /rental-action` (pause/unpause/trash). Free to list; tier carried for future paid tiers. Shared `sb_apply_rental_fields` writer. Reads native ACF `rental` CPT (`group_lima_rental`: daily/hourly/**rate_unit**/deposit/inclusions/provider/gallery/map/available/**facebook**/**instagram**) + `rental-type` taxonomy. Reuses `sb_acf`/`sb_img_url`. Live & verified 2026-06-03. |
| `limasawa-edit.php` | Host listing EDIT: `GET /listing-edit/{id}` (JWT owner → listing in the form's `data`-model shape so `/list-your-property?edit=ID` prefills every step) + `POST /update-listing/{id}` (JWT owner → updates title/content + ACF, **leaves tier/`sb_payment` untouched** — edits go live immediately, no re-approval). Defines **`sb_apply_listing_fields()`** — the single shared form→ACF writer also called by `limasawa-submit.php`'s create path (refactored to delegate). Owner-scoped via `sb_edit_guard()` (403 non-owner, 401 unauth). Live & verified 2026-06-03. |
| `limasawa-dashboard.php` | Dashboard data + analytics: `GET /my-listings`, `POST /listing-action` (pause/unpause/trash), `POST/DELETE /save-listing` + `GET /my-saved`, `POST /track/view` + `/track/click` (per-listing `sb_stats_daily`, dedup+bot filter+source bucket), `GET /host-stats` (period aggregation), `GET /listing-stats`. JWT, owner-scoped. Live & verified 2026-06-01. |
| `limasawa-reviews.php` | Reviews as WP comments (`sb_review`, host-approval): `GET/POST /reviews`, `GET /reviews/pending`, `POST /reviews/{id}/approve|reject`, `/reply`, `GET /my-reviews`, `POST /reviews/{id}` (edit). Per-listing `sb_rating_avg/count` recalc → card pill + analytics KPI. Live & verified 2026-06-01. |
| `limasawa-posts.php` | Host blog: dashboard Posts panel (`GET /my-posts`, `POST /create-post`, `GET/POST/DELETE /post/{id}`; WP posts; EditorJS HTML→post_content + JSON→`sb_content_blocks`) **+ public blog read API** (`GET /blog/posts` paged/category/search, `GET /blog/categories`, `GET /blog/post/{slug}` with content/categories/author/related). **Blog writing is tier-gated**: `sb_can_blog()` requires Pro+ (`sb_host_max_tier()` = best tier across the host's listings; admin/editor bypass) — enforced on create/update-post (403 `TIER_LOCKED`). Live & verified 2026-06-03. |
| `limasawa-admin.php` | WP-admin CMS (rich port of siargao's sb-dashboard-widget.php, adapted to limasawa meta): Analytics dashboard widget = stat tiles (active-now, views 7d/30d, signups, listings, reviews-waiting, payments/MRR) + 30d SVG views chart + payments-to-verify + expiring-soon + recent listings + signups + posts-to-approve + composite top-listings + pending-claims section. approve-payment→paid+publish+tier+expiry, approve/reject posts, list filters (tier/payment), per-listing meta box, "Limasawa" CMS menu page, admin columns. Cap `edit_others_posts`. Impersonation omitted. Live 2026-06-01. |
| `limasawa-claims.php` | Listing claims (ported siargao sb-claims.php): `POST /claim-listing`, `GET /my-claims`, `wp_sb_claims` table, admin approve/reject (transfers post_author + seeds avatar), pending-claims table in CMS widget. Adapted to limasawa JWT + `sb_profile_picture_id`. Live & verified 2026-06-01. |
| `limasawa-admin-login.php` | Admin-subdomain entry: branded wp-login (Limasawa wordmark + cyan), root `/` → `/wp-admin` when logged in / branded login when out; skips REST/cron/wp-admin. Live 2026-06-01. |
| `limasawa-no-public-access.php` | Keeps the WP subdomain out of search: `X-Robots-Tag: noindex` on public responses, robots.txt `Disallow: /` (allow `/wp-json/`), and **path-based** 301s (`/accommodation/{slug}` → `limasawabooking.com/stay/{slug}/`, `/place` → `/stays/`, else → home). Skips `/wp-json`,`/wp-admin`,`/wp-login`,cron + the root (owned by admin-login). Live & verified 2026-06-01. |
| `limasawa-coupons.php` | Coupons (ported siargao sb-coupons.php): `wp_sb_coupons` table (migrates demo `WELCOME10`/`SAVE500` from the old `sb_coupons` option), wp-admin **Coupons** CRUD page + REST `limasawa/v1/admin/coupons`, `sb_validate_and_apply_coupon()` (active/expiry/usage-limit/tier/billing + bonus months). `/coupon/validate` + renew delegate to it; approve-payment applies bonus months + increments usage. Live & verified 2026-06-01. |
| `limasawa-manage-hosts.php` | WP-admin "Hosts" page (ported siargao sb-manage-hosts.php): filterable table of accommodation authors (listing counts by status, tier, MRR, 30d views, rating, flags) + slide-in drawer (Listings/Subscriptions/Claims/Activity/Notes) + suspend toggle. REST `limasawa/v1/admin/host/{id}` (+`/notes`,`/suspend`), cap `manage_options`. Impersonation omitted; suspend sets `sb_user_suspended` meta — **enforced** in `limasawa-auth.php` (login → 403 SUSPENDED, JWT resolver → null). Live & verified 2026-06-01. |
| `limasawa-cron.php` | Daily `sb_subscription_check`: paid listings past expiry+3-day grace → downgrade to free (`sb_payment.status=expired`, `listing_tier=free`, stays published) + lapse email; expiring within 7 days → one warning email (transient-guarded). Ported from siargao lifecycle, adapted to `sb_payment` array. Has `$dry` mode. Live & verified 2026-06-01. |
| `limasawa-deploy-hook.php` | **Frontend auto-rebuild trigger.** The static Astro build prebuilds every `/stay/{slug}` + `/rental/{slug}`, so a new listing 404s (and rental edits don't show) until a rebuild. On `transition_post_status` (publish / edit-while-published / unpublish / trash) + `before_delete_post` for `accommodation`+`rental`, pings a Vercel **Deploy Hook** (`wp_remote_post`, non-blocking) → rebuilds prod from GitHub `main` → live ~1–2 min later. Debounced: first save fires immediately + opens a `SB_DEPLOY_COOLDOWN` (60s) window; saves inside it set a dirty flag + schedule one trailing build (`sb_deploy_flush`). Hook URL read from `SB_DEPLOY_HOOK_URL` constant (wp-config) if set, else `sb_deploy_hook_url` wp_option. Live & verified 2026-06-07 (real WP edit → fresh git deploy confirmed). |

### Secrets / data notes
- JWT secret `SB_JWT_SECRET` in `wp-config.php` (not in git). Coupons live in the `sb_coupons` wp_option (demo: `WELCOME10` 10%, `SAVE500` ₱500). Per-user meta: `sb_saved`, `sb_contact`, `sb_profile_picture_id`, `sb_review_count`. Per-listing meta: `listing_tier`, `sb_payment` (array), `sb_stats_daily`, `sb_rating_avg/count`, `sb_paused`. Reviews = `sb_review` comments; host blog = native WP posts. **Vercel deploy-hook URL** in the `sb_deploy_hook_url` wp_option (build-trigger secret, not in git; `limasawa-deploy-hook.php` pings it on listing/rental publish/edit).

### Data model (native ACF — read, don't hand-roll)
- **CPT:** `accommodation` (25 published as of 2026-05-31). Field group `group_668c1326c9162` (38 fields, prefix `accommodation_*`).
- **Taxonomies:** `accommodation-category`, `accommodation-amenity`, `accommodation-exclusion`, `location` (in the listings formatter, location term[0]=municipality, term[1]=barangay).
- Key fields: `accommodation_daily_rent`, `accommodation_monthly_rent`, `accommodation_bedrooms/beds/bathrooms`, `accommodation_number_of_guests`, `what_type_of_place_will_guests_have`, `accommodation_gallery` (gallery), `accommodation_location` (google_map → lat/lng), `accommodation_host_picture` (image), `accommodation_badges` (checkbox), `faq_amenities` (group), `services` (repeater gated by `add_services`).
- **Pricing fields (added to this group 2026-06-01, after `accommodation_monthly_rent`):** `pricing_mode` (select flat/rooms/per_pax), `accommodation_room_types` (repeater: `room_name`/`room_capacity`/`room_rate`/`room_note`, shown when rooms), `additional_bed_rate` (rooms), `per_pax_base_rate`/`per_pax_base_pax`/`per_pax_extra_rate`/`per_pax_max_pax` (per_pax), `pricing_extras` (repeater: `extra_label`/`extra_amount`/`extra_note`, any mode). Field keys are `field_lima_*`.

## MCP / Tooling
- `novamira-admin-limasawabooking-com` — MCP connected to admin subdomain

## Files

### Components (`src/components/`)
| File | Purpose |
|---|---|
| `SiteNav.astro` | Sticky header — logo, Browse Stays, List your property, account dropdown |
| `SiteFooter.astro` | Footer with nav columns |

### Pages (`src/pages/`)
| File | Purpose |
|---|---|
| `index.astro` | Homepage — search-first hero, featured `AccommodationCard` grid, browse-by-type/location tiles, host CTA, about. Search + tiles link to `/stays?location=&category=` |
| `stays.astro` | Thin wrapper → renders `StaysExperience` |
| `list-rental.astro` | Host rental submission form (dedicated, auth-gated): create + `?edit=ID`. Title, type, daily/hourly/deposit, inclusions, provider, barangay, photos (`/upload-media`), description → `POST /submit-rental` (pending) / `POST /update-rental/{id}`. Provider prefilled from JWT username. |
| `rentals/index.astro` + `rental/[slug].astro` | **Rentals vertical** front-end: `/rentals` archive (client-fetches `/rentals` + `/rental-types`, type-filter chips) and `/rental/{slug}` single (gallery, day/hour rates + deposit, inclusions, provider Call/WhatsApp, keyless OpenStreetMap pin; `getStaticPaths` prebuilds each). Card = `src/scripts/rental-card-template.js` + `src/styles/rental-card.css`. Linked in SiteNav + footer. |
| `blog/index.astro` + `blog/[slug].astro` | Public blog (ported from siargao, rebranded teal): `/blog` archive (client-fetches `/blog/posts` + `/blog/categories`, search + category chips + load-more) and `/blog/{slug}` single post (`getStaticPaths` prebuilds every published post for SEO; renders content + author + related). Shared card = `src/scripts/blog-card-template.js` + `src/styles/blog-card.css`. Footer links `/blog`. |
| `stay/[slug].astro` | Single listing template (ported from siargao) — photo gallery, facts, amenities, reviews shell, JSON-LD `LodgingBusiness`. `getStaticPaths` via `getAccommodations` |
| `auth.astro` | Login / register (JWT) |
| `dashboard/index.astro` | Host dashboard — listings panel |
| `list-your-property.astro` | Conversational multi-step listing form (ported from siargao list-your-home): tier→billing→host→property→category→location→capacity→rates→description→links→services→amenities→generator→gallery→payment→review→success. Leaflet+Nominatim address step; uploads via `/upload-media`; submits via `/submit-listing`; coupon via `/coupon/validate`. Live 2026-06-01. |
| `404.astro` | 404 page |

### Components & support files
| File | Purpose |
|---|---|
| `src/components/StaysExperience.astro` | Full `/stays` archive (ported from siargao): Leaflet map + marker cluster (CartoDB tiles, NO API key), filter drawer, category pills, load-more. Client-fetches `/listings`, `/map-pins`, `/taxonomies` |
| `src/components/AccommodationCard.astro` | Listing card (ported). Renders via `src/scripts/card-template.js` (`cardHTML`) + `card-slider.js` + `src/styles/card.css` |
| `src/lib/api.ts` | REST client: flat `getListings`/`getListing`/`getMapPins`/`deriveFacets` + `getAccommodations`/`reshapeListing` (nested shape the ported components consume) |
| `src/lib/locations.ts` | `parseLocationNames` — positional muni/barangay split (term[0]/term[1]) |
| `src/layouts/Layout.astro` | Base HTML shell, `window.__sb` saved-listings singleton |
| `src/styles/global.css` | Design tokens + utility classes; `@import "./card.css"` |
| `.env.example` | `PUBLIC_WP_REST`, `PUBLIC_GOOGLE_MAPS_KEY` (the ported `/stays` uses Leaflet/OSM and needs no key) |

## Status (as of 2026-05-31)
- Scaffold complete — all core pages created
- **Auth layer LIVE**: guest/host roles, JWT, `/auth/register`, `/auth/login`, `/me` — verified end-to-end over HTTP. Code in `wp/sandbox/limasawa-auth.php`.
- **Listings REST API LIVE** (`wp/sandbox/limasawa-listings.php`): `/listings` (rich filters: category/cats/type/badge/rate_mode/price_min/price_max/beds/baths/amenities/location/bounds + `server_filters:true`), `/listings/{slug}`, `/map-pins` (bare array), `/taxonomies`. Verified over HTTP against 25 published accommodations. Filtering: clean tax/meta via WP_Query, price/type/badge/bounds post-filtered in PHP (fine at ~25 listings).
- **Card / `/stays` archive / single template ported from siargaobooking** and adapted to limasawa (2026-05-31). Card design matches siargao. `/stays` = `StaysExperience` (Leaflet map, no API key). Build = 31 pages, all live.
- **Reviews/ratings LIVE** (`limasawa-reviews.php`): host-approval moderation, card rating pill + analytics rating KPI wired. (`/my-claims` from the ported single template is the one remaining unbuilt endpoint — claim-listing flow; degrades silently.)
- **`auth.astro` LIVE & wired** to `/auth/login` + `/auth/register` ({email,password} → `authToken` in `localStorage.sb_token`), with backend error codes mapped to friendly messages. `/me` extended (GET) + `POST /me` (account/photo save). Verified against live backend.
- **Full host/guest DASHBOARD LIVE** (`/dashboard`, ported siargao UI, 2026-06-01): my-listings + actions (pause/trash), saved listings (server-synced — `window.__sb` now persists), account edit + optional photo (face-gate dropped), analytics (real view/click tracking; history accrues from launch), orders/subscription renewal (tier upgrades pending admin approval), reviews (pending/my-reviews), host blog. Tier-gating off `listing_tier`. Backends: `limasawa-dashboard.php` + `limasawa-reviews.php` + `limasawa-posts.php` + `limasawa-admin.php` (WP-admin approve-payment→publish). All verified over HTTP.
- **`list-your-property` LIVE**: full conversational form ported from siargao + `limasawa-submit.php` backend (verified end-to-end: coupon math, media upload, submit→pending with correct ACF/taxonomy mapping). Submissions land as `pending` accommodations (admin approves → publish). Payment QR images live at `admin.limasawabooking.com/wp-content/uploads/2026/05/{gcash,maya,bpi}-number.webp` (verified 200; GCash/Maya 09260367249, BPI 5739059781, Nico Lanugon) — shown on both the accommodation payment step and the rentals payment block (with `onerror` graceful-hide). Demo coupons `WELCOME10`/`SAVE500` seeded in `sb_coupons` option; Google Places autocomplete needs Places API enabled on the key (Nominatim geocode works without it); `faq_hot_&_cold_shower` ACF subfield name has an `&` so that one amenity toggle won't map (9/10 faq amenities map fine).
- Homepage mockup target (search + tours/rentals/transport/food/travel-guide verticals) needs new backend content types (only `accommodation` exists today) — out of scope until those CPTs exist
- **Login/register redirect-loop FIXED** (2026-06-01): dashboard auth gate checked `meData.success`, but limasawa `/me` returns `{status:'ok', user}` → `doLogout()` fired and bounced back to `/auth`. Now checks `meData.user` (`dashboard/index.astro:3771`).
- **Flexible pricing LIVE** (2026-06-01): per-listing `pricing_mode` = flat (existing) | rooms (room-types repeater + additional-bed) | per_pax (base+extra/pax tiered), **plus** a universal `pricing_extras` repeater (free-text label + amount add-ons, e.g. Adult ₱300, any mode). **Fields are NATIVE ACF in the DB "Accommodation" field group (`group_668c1326c9162`, post ID 14), inserted right after `accommodation_monthly_rent` — editable in the ACF UI.** (Originally registered as a code group `limasawa-rates.php`; migrated into the DB group 2026-06-01 per user — that file is removed. Schema exported to `wp/acf-json/group_668c1326c9162.json` for git versioning; rollback backup in wp_option `lima_acf_accom_backup`.) `sb_rate_summary()` in listings.php computes `priceFrom`; submit form has a pricing-mode chooser → conditional rate sub-steps + an optional "Extra charges" step (`list-your-property.astro`); cards show "from ₱X" (`card-template.js`, threaded via `api.ts` `priceMode`); single page renders the breakdown in the right booking card (`stay/[slug].astro` `renderSidebarRates`). For multi-rate listings daily/monthly rent are ignored (only used in `flat`). Verified rooms→₱1,999-from, per_pax→₱3,400, + extras round-trip over PHP. ⚠️ ACF gotcha learned: `acf_import_field_group()` with an existing group key CREATES DUPLICATE groups (same key) rather than updating — to add fields to a DB group, save them surgically via `acf_update_field()` with `parent`=group post ID + `menu_order` (shift later fields first).
- **Host edit-listing LIVE** (2026-06-03, `limasawa-edit.php`): dashboard "Edit listing" → `/list-your-property?edit=ID`; form fetches `GET /listing-edit/{id}` to prefill, saves via `POST /update-listing/{id}` (owner-scoped, 401/403/404 guarded, tier/payment untouched). The form→ACF mapping is now a single shared `sb_apply_listing_fields()` (create + edit both call it; submit.php refactored). Verified full round-trip + ownership over PHP. ⚠️ Minor: location terms are stored flat (a single barangay term, e.g. "Cabulihan"), so on edit the municipality/barangay chips may not perfectly prefill — but the existing location term is preserved on save (only overwritten if a barangay is re-picked, `barangayId>0`).
- **Public blog LIVE** (2026-06-03, ported from siargao): `/blog` + `/blog/{slug}`, public read API in `limasawa-posts.php`, **tier-gated writing** (Pro+, admin bypass, 403 `TIER_LOCKED`). Seeded one starter article ("How to Get to Limasawa Island") + trashed WP's default "Hello world!". Verified live (200s, SEO titles, static-prebuilt content).
- **Rentals vertical LIVE** (2026-06-03): new native ACF `rental` CPT + `rental-type` taxonomy + `group_lima_rental` fields + shared `location` taxonomy. Backend `limasawa-rentals.php`; frontend `/rentals` + `/rental/{slug}` (+ nav/footer links). Verified live end-to-end.
- **Rentals model overhaul** (2026-06-10): **one listing = one vehicle type at one price**. `rental-type` now Scooter/Motorbike/Car/Van/**Boat**/**Jet Ski**(+Bicycle/Tricycle). Added `rental_rate_unit` ACF select (day/hour/trip/person, key `field_lima_rental_rate_unit`, in group_lima_rental id 754) so boats/jetskis price per hour/trip/person — exposed as `rateUnit` in card/single/edit REST shapes, written by the form, rendered on cards (`rental-card-template.js` `rtUnitLabel`) + single (`rental/[slug].astro` `unitLabel`). **All 11 existing rentals retagged** (9 Scooter, 1 Van=Avanza door-to-door transfer/trip, 1 Boat=Chariot ₱250/person — Chariot type was a guess). `/rentals` archive now has working type chips + **price filter (buckets) + sort** (recommended/price-asc/desc, client-side over the fetched list). **New tiers**: Free=1 / **Verified (pro) ₱99/mo (₱990/yr)=up to 3** / **Featured ₱149/mo (₱1,490/yr)=unlimited** — `sb_rental_fleet_caps()`=[free 1, pro 3, featured 0], `sb_rental_tier_prices()` updated; `/list-rental` plan cards + amounts + static full type list + rate-unit selector. (Internal tier keys stay free/pro/featured.)
- **Button hover tidy-up** (2026-06-11): two real hover bugs — (1) SiteNav's `.nav-links a:hover` outranked `.btn-primary` (specificity 0,1,1 > 0,1,0) and painted the "List your property" CTA text teal-on-teal; scoped to `a:not(.btn-primary)`. (2) Homepage `.btn-light-outline { color:#fff }` was defined *before* `.btn-light { color: deeper-teal }` (equal specificity → later wins) so "List your rental" rendered teal-on-teal looking disabled; doubled to `.btn-light.btn-light-outline`, hover now inverts to white bg + deep teal text, and `.btn-light` gained a matching 1.5px border so both CTA buttons are equal height. Global `.btn-primary/secondary/outline` harmonized: consistent hover lift, `:active` reset, `:focus-visible` rings, primary hover shadow re-tokened (was hardcoded to the old lighter cyan).
- **Stray map pin + coords hardening** (2026-06-11): a host-submitted listing (J-Room, id 1213, created 06-10) briefly had a bad map pin near Libagon (mainland); corrected in WP 06-11 01:41, but `/stays` kept showing it from the **sessionStorage pin cache (`sb_pins_v1`) which never revalidated**. Fixes: (1) `fetchPins` in `StaysExperience.astro` is now stale-while-revalidate — cached pins paint instantly, a background fetch updates cache + re-renders markers; (2) **server-side coords clamp**: `sb_on_limasawa()` + `sb_limasawa_safe_coords()` (owner: `limasawa-listings.php`) — out-of-island lat/lng from submit/edit falls back to the barangay centroid (else island center) in BOTH `sb_apply_listing_fields` (edit.php) and `sb_apply_rental_fields` (rentals.php); (3) frontend `MUNI_COORDS` centroids re-derived from real listing GPS (kept in sync with the PHP table). Deployed to server + verified over PHP (Libagon→magallanes centroid round-trip on a draft) and REST 200s; all 44 live pins in-bounds.
- **Payment-proof validation FIXED both forms** (2026-06-11): paid submissions now accept **method + (reference no. OR receipt)** — previously `/list-rental` hard-required a typed reference (uploaded receipt didn't count → user blocked with receipt uploaded) and `/list-your-property` hard-required a receipt. Backends never required either (just store them). Also fixed in `list-rental.astro`: `init()` was called **before** the module's `let/const` state declarations — a TDZ ReferenceError swallowed by async whenever the JWT payload had `data.user.username` (real tokens only carry `uid`/`role`, so prod survived by short-circuit luck); gate moved below declarations, and the never-working provider prefill (read a nonexistent JWT claim) now fetches `/me`. Also fixed in `list-your-property.astro`: the payment-reference input's inline `oninput` referenced module-scoped `data` → ReferenceError every keystroke since launch (`paymentReference` always submitted empty); now `onPaymentReferenceInput()` exposed via the `Object.assign(window,…)` block. All verified in browser (stubbed API): rental pro+receipt-only submits, no-proof blocks, accommodation pro reference-only enables Continue.
- **Rental social links** (2026-06-10): added `rental_facebook` + `rental_instagram` ACF url fields (keys `field_lima_rental_*`, group_lima_rental 754). Exposed as `facebook`/`instagram` in rental REST shapes, written by `sb_apply_rental_fields` (`esc_url_raw`), form inputs on `/list-rental` (create+edit), and FB/IG buttons in the `/rental/{slug}` provider card — shown for **all tiers**.
- **Rentals host self-listing LIVE** (2026-06-03): free to list → pending → admin publishes (wp-admin). `/list-rental` dedicated form (create + edit) + a **"My rentals"** dashboard panel (host-only nav `data-tab="rentals"`, status badges, edit/pause/unpause/delete). Backend `submit-rental`/`my-rentals`/`rental-edit`/`update-rental`/`rental-action`. **Paid tiers LIVE** (2026-06-04; **repriced 2026-06-10** → Free=1 / Verified ₱99=3 / Featured ₱149=unlimited, see "Rentals model overhaul" above). Backend: `submit-rental` enforces fleet caps by requested tier (`sb_rental_fleet_caps`), records `sb_payment` (pending_review for paid; live `listing_tier` stays free until admin verifies in wp-admin), `/rentals` sorts featured>pro>free. Form: plan cards + monthly/yearly toggle + payment block (method/reference/receipt) for paid; cards show Featured/Verified badges. **Returning-host 50% discount**: a paid rental is half-price when the host already has ≥1 listing (accommodation or rental) — computed server-side in submit-rental (`baseAmount` + `multiListingDiscount` in sb_payment), mirrored in the form (banner + halved plan prices/amount). **CTAs:** "List your rental" on homepage host section + footer + a provider band on /rentals. Verified: fleet limit 403, pro submit→pending_review, 1st paid=₱399 / 2nd paid=₱199, plan UI in-browser. **CMS one-click approve LIVE for rentals** (2026-06-04): `sb_admin_do_approve` now handles the rental CPT + a "Rentals to verify" table in the dashboard widget reuses `sb_approve_payment` → publishes + sets `listing_tier` + stamps expiry (verified: pending featured rental → publish + tier featured + paid + +30d expiry).
- **Editor/admin dashboard FIXED** (2026-06-04): `isHostRole()` now covers host/admin/administrator/editor, so admin/editor accounts get the full host dashboard (was showing guest view).
- **List-your-property tweaks** (2026-06-04): municipality step removed (only Limasawa; defaulted) + full-description (Quill) editor capped so OK/Skip stay reachable.
- **Homepage mockup verticals** now: accommodations ✅ + rentals ✅ live; tours/transport/food still TODO (each needs its own CPT/vertical like rentals).
- **SEO landing pages LIVE** (2026-06-04, `src/pages/stays/[slug].astro`): static per-barangay pages (`/stays/{cabulihan,lugsongan,magallanes,san-agustin,san-bernardo,triana}`) + `/stays/beachfront` (filters on the listings-API `beachfront` flag = `accommodation-amenity` term beach-access/beach-view **OR** the listing form's "Beach access" FAQ toggle `faq_amenities.faq_beach_access`). Each: keyword title/H1/intro + matching `AccommodationCard`s (build-time via `getListings({loc})` + exported `reshapeListing`) + "Browse by area" cross-links + a map link. Homepage "browse by location" tiles repointed to these. In sitemap. Add category landings (homestays/resorts) the same way later.
- **SITE IS LIVE** at https://limasawabooking.com (Cloudflare proxies the apex → Vercel; serving the real homepage + listings as of 2026-05-31). Deploy via `vercel --prod --yes` (project not git-auto-deploying). Note: Vercel reports a nameserver mismatch (wants ns*.vercel-dns.com, domain stays on Cloudflare NS) but Cloudflare proxy A/CNAME records make it resolve fine — leave as-is.
